#include <bits/stdc++.h>
using namespace std;

const int N = 1024;

int p = 0, dp[20][2];
char str[N];

void solve(int dep) {
    char ch = str[p];
    if (ch == '[') {
        
    }
}

int main() {
    int T;
    scanf("%d", &T);
    while (T--) {
        scanf("%s", str);
        p = 0;
        memset(dp, 0, sizeof(dp));
    }
}